const ProductCarousel = ({height, width, background, nameOfClass}) => {
    return(
        <div style={{height: height, width: width, backgroundColor: background}} className={nameOfClass}>
            HEJ
        </div>
    );
};

export default ProductCarousel;